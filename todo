  ------------ Нужно запустить ----------------

 - переписать код чтения VDR и VarsFile. ClassData тоже стоило бы (через switch);

 - Возможно поправить алгоритм присвоения Z для соответствия с стратумом;
 - разобраться с флагами;

И общие задачи:
 - почистить any везде где есть;
 - навести порядок с ошибками.
 - не использовать map-ы где можно обойтись обычным find

Что оптимизировать (номера проблемных строк кода):
 - SendMessage, попробовать физически связать переменные вместо копирования;

- Ошибки при загрузке изображений нужно перехватывать и гасить;
- Любые ошибки с файлами/имиджами и т.д. нужно гасить;

Директории в стратуме БЕЗ СЛЕША В КОНЦЕ!!

 - везде где используются пути сделать нормализацию, учитывать, что они могут быть относительные.
 ЮНИКС ПУТИ ТОЖЕ РАБОТАЮТ, УЧЕСТЬ ЭТО. Т.е. нормально обрабатывать как / так и \ пути, но возвращать только \.

Гиперджампы:
 - в гипербазе: при показе схемы объекта размещать ее на сцене в виде отдельного объекта (не смешивать с объектами сцены);
 - создать хранилище матриц;
 - продумать механизм переключения между проектами.
 - реализовать механизм подмены/создания нового активного окна, дабы загружать открывать проекты без перезагрузки окна браузера.
 - асинхронное считывание данных: динамическая подгрузка имиджей, изображений.

Окна:
 - разобраться с осциллографом;
 - сделать элементарную оконную систему;
 - запустить проект smart_house;


Переделать при помощи подиски на события: Scene должна подписываться на Renderer.
Универсиализировать класс Renderer.
Обеъдинить Scene и Renderer?